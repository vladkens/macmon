# Demo app

This standalone Cargo application demonstrates a continuous monitoring loop with the minimal `macmon::Sampler` API. It prints ECPU, PCPU, and GPU active percentages, CPU and GPU temperatures, plus RAM usage.

The presentation is inspired by [homm/pgauge](https://github.com/homm/pgauge).
